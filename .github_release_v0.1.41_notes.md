## English
### Highlights
1. Added deterministic BOOT apply command path:
   - registered `/gov_apply <NN>` in plugin command layer
   - added runner: `tools/gov_apply_sync.mjs`
2. Expanded executable runtime regression baseline to `28/28` with apply-focused cases:
   - invalid item input
   - missing BOOT menu context
   - QC recurrence apply path
   - Guard escalation apply path
3. Strengthened release consistency gates:
   - apply runner existence check
   - apply command registration/wiring checks
   - public-flow `Phase B5` matrix presence checks
4. Added governance engineering continuity docs:
   - `dev/GOVERNANCE_MASTER_SPEC.md`
   - `dev/GOVERNANCE_TRACEABILITY_MATRIX.md`
   - `dev/GOVERNANCE_GAP_REGISTER.md`
   - `dev/SESSION_HANDOFF.md`
5. Updated maturity docs:
   - `gov_apply` remains Experimental (controlled-UAT) but deterministic-covered

### Validation
1. `node dev/check_release_consistency.mjs` -> `ALL_CHECKS_PASS`
2. `node dev/run_runtime_regression.mjs` -> `SUMMARY 28/28 passed`

### Upgrade Path
1. `openclaw plugins update openclaw-workspace-governance`
2. `openclaw gateway restart`
3. In TUI: `/gov_setup check`
4. If needed: `/gov_openclaw_json` -> `/gov_setup check`
5. Continue: `/gov_setup upgrade` -> `/gov_migrate` -> `/gov_audit`
6. Experimental BOOT apply path:
   - approve one BOOT item
   - run `/gov_apply <NN>`
   - close with `/gov_migrate` -> `/gov_audit`

## 繁體中文
### 重點更新
1. 新增決定性 BOOT apply 指令路徑：
   - 在插件命令層註冊 `/gov_apply <NN>`
   - 新增執行器：`tools/gov_apply_sync.mjs`
2. 可執行 runtime regression 基線擴展至 `28/28`，並補強 apply 情景：
   - 無效 item 輸入
   - BOOT menu context 缺失
   - QC recurrence apply 路徑
   - Guard escalation apply 路徑
3. 強化 release consistency gate：
   - apply runner 存在性檢查
   - apply 指令註冊/接線檢查
   - public-flow `Phase B5` 矩陣存在性檢查
4. 新增治理工程延續文件：
   - `dev/GOVERNANCE_MASTER_SPEC.md`
   - `dev/GOVERNANCE_TRACEABILITY_MATRIX.md`
   - `dev/GOVERNANCE_GAP_REGISTER.md`
   - `dev/SESSION_HANDOFF.md`
5. 成熟度定位更新：
   - `gov_apply` 仍為 Experimental（受控 UAT），但已納入決定性覆蓋

### 驗證結果
1. `node dev/check_release_consistency.mjs` -> `ALL_CHECKS_PASS`
2. `node dev/run_runtime_regression.mjs` -> `SUMMARY 28/28 passed`

### 升級步驟
1. `openclaw plugins update openclaw-workspace-governance`
2. `openclaw gateway restart`
3. 在 TUI 執行：`/gov_setup check`
4. 如需要：`/gov_openclaw_json` -> `/gov_setup check`
5. 繼續：`/gov_setup upgrade` -> `/gov_migrate` -> `/gov_audit`
6. Experimental BOOT apply 路徑：
   - 先核准一個 BOOT item
   - 執行 `/gov_apply <NN>`
   - 收口 `/gov_migrate` -> `/gov_audit`
