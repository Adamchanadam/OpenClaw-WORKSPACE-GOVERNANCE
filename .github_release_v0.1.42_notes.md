## English
### Highlights
1. Fixed setup/migrate loop on first adoption:
   - `/gov_setup install|upgrade` now routes operators to `/gov_migrate -> /gov_audit`
   - removed bootstrap-document bounce in deterministic command output
2. Added deterministic migrate auto-reconcile for missing governance baseline files:
   - auto-seed `_control/GOVERNANCE_BOOTSTRAP.md`
   - auto-seed `_control/REGRESSION_CHECK.md`
3. Added deterministic marker repair path:
   - when required AUTOGEN marker is missing, runner repairs target from canonical payload (with backup), then continues
4. Expanded operator evidence:
   - migrate output now reports `seeded_missing_files` and `repaired_missing_marker_files`
5. Regression and docs alignment:
   - updated runtime regression and release consistency gates to prevent this loop from regressing
   - synced README / WORKSPACE_GOVERNANCE_README / skills contracts with implemented flow

### Validation
1. `node dev/run_runtime_regression.mjs` -> `SUMMARY 28/28 passed`
2. `node dev/check_release_consistency.mjs` -> `ALL_CHECKS_PASS`

### Upgrade Path
1. `openclaw plugins update openclaw-workspace-governance`
2. `openclaw gateway restart`
3. In TUI: `/gov_setup check`
4. If needed: `/gov_openclaw_json` -> `/gov_setup check`
5. Continue: `/gov_setup upgrade` -> `/gov_migrate` -> `/gov_audit`

## 繁體中文
### 重點更新
1. 修復首次導入時的 setup/migrate loop：
   - `/gov_setup install|upgrade` 現在會引導至 `/gov_migrate -> /gov_audit`
   - 移除 deterministic 回覆中的「先跑 bootstrap 文件」回彈路徑
2. 新增 migrate 決定性自動補齊缺失基線檔：
   - 自動補 `_control/GOVERNANCE_BOOTSTRAP.md`
   - 自動補 `_control/REGRESSION_CHECK.md`
3. 新增 AUTOGEN marker 自動修復路徑：
   - 若目標檔缺 marker，runner 會先備份，再以 canonical payload 修復後繼續
4. 補強操作證據輸出：
   - migrate 會回報 `seeded_missing_files` 與 `repaired_missing_marker_files`
5. 回歸測試與文檔對齊：
   - 已更新 regression 與 consistency gate，防止同類 loop 再發
   - README / WORKSPACE_GOVERNANCE_README / skills 契約已同步至實作流程

### 驗證結果
1. `node dev/run_runtime_regression.mjs` -> `SUMMARY 28/28 passed`
2. `node dev/check_release_consistency.mjs` -> `ALL_CHECKS_PASS`

### 升級步驟
1. `openclaw plugins update openclaw-workspace-governance`
2. `openclaw gateway restart`
3. 在 TUI 執行：`/gov_setup check`
4. 如需要：`/gov_openclaw_json` -> `/gov_setup check`
5. 繼續：`/gov_setup upgrade` -> `/gov_migrate` -> `/gov_audit`
