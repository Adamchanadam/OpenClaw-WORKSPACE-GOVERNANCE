## English
### Highlights
1. Root-fixed `gov_uninstall` cleanup scope:
   - removed broad recursive delete behavior on shared folders
   - cleanup now targets explicit governance artifacts only
2. Added Brain Docs uninstall evidence path:
   - `check` now reports `brain_docs_backup_roots_found`, restore candidates, and strategy
   - `uninstall` now reports `brain_backup_used` and `brain_backup_strategy`
3. Hardened uninstall safety for mixed workspaces:
   - preserves non-governance custom files under shared parents (for example `prompts/governance/*` user files)
4. Expanded executable runtime regression:
   - new uninstall integrity cases added
   - denominator baseline moved to `33/33`
5. Synced dev docs and release gates to latest behavior:
   - `SESSION_HANDOFF`, `MASTER_SPEC`, traceability matrix, public-flow regression, and publishing baseline

### Validation
1. `node dev/run_runtime_regression.mjs` -> `SUMMARY 33/33 passed`
2. `node dev/check_release_consistency.mjs` -> `ALL_CHECKS_PASS`

### Upgrade Path
1. `openclaw plugins update openclaw-workspace-governance`
2. `openclaw gateway restart`
3. In TUI: `/gov_setup check`
4. Continue: `/gov_migrate` -> `/gov_audit`

## 繁體中文
### 重點更新
1. `gov_uninstall` 清理範圍完成 root-fix：
   - 移除 shared folder 的 broad recursive delete 行為
   - 改為只清理明確治理清單目標
2. 新增 Brain Docs 卸載證據鏈：
   - `check` 會輸出 `brain_docs_backup_roots_found`、回復候選與策略
   - `uninstall` 會輸出 `brain_backup_used`、`brain_backup_strategy`
3. 強化混合工作區卸載安全：
   - 保留 shared parent 下的非治理自訂檔（例如 `prompts/governance/*` 用戶檔）
4. 擴展可執行 runtime regression：
   - 新增 uninstall 完整性測試案例
   - 分母基線更新為 `33/33`
5. `dev` 文檔與 release gate 已同步到最新行為：
   - `SESSION_HANDOFF`、`MASTER_SPEC`、traceability matrix、public-flow regression、publishing baseline

### 驗證結果
1. `node dev/run_runtime_regression.mjs` -> `SUMMARY 33/33 passed`
2. `node dev/check_release_consistency.mjs` -> `ALL_CHECKS_PASS`

### 升級步驟
1. `openclaw plugins update openclaw-workspace-governance`
2. `openclaw gateway restart`
3. 在 TUI 執行：`/gov_setup check`
4. 繼續：`/gov_migrate` -> `/gov_audit`
